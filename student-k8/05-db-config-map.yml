apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  POSTGRES_HOST: "postgres-service"   # This is the HEADLESS service name!
  POSTGRES_PORT: "5432"
  POSTGRES_NAME: "studentdb"           # Must match POSTGRES_DB in the secret
