apiVersion: v1
kind: Service
metadata:
  name: student-app-service
spec:
  type: NodePort # Use NodePort if you're on Minikube without tunnel
  selector:
    app: student-app
  ports:
    - port: 80
      targetPort: 8080
